"""safesc — supply-chain trust auditing.

Single top-level package for the SafeSC runtime. All internal packages
(``tools``, ``graph``, ``entrypoints``, ``reporter``, ``memory``, ``security``) live
under this namespace so they no longer occupy generic global import names once installed.
"""
